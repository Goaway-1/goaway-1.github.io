---
layout: post
title: 코딩테스트 | 물 웅덩이
subtitle: BFS
categories: CodingTest
tags: [CodingTest, C++]
---

## 문제

  여름 장마철!!! 철수의 학교에는 비가 억수같이 내렸다. 비가 멈추고 난 후 교장 선생님은 운 동장을 둘러보았는데 여기 저기 물 웅덩이가 생겨 학생들이 쉬는 시간에 운동장에서 뛰어 놀 지 못 할 것 같아 웅덩이를 흙으로 메우려고 한다.

  우선 운동장에 물 웅덩이가 몇 개나 있는지 교장선생님은 알고 싶다. 여러분이 교장선생님을 도와 운동장에 물 웅덩이가 몇 개나 있는지 알아내는 프로그램을 작성하세요.학교의 운동장은 R*C크기의 격자로 주어지며, 각 웅덩이는 #으로 표기하되 그 크기는 1개 아 니면 상하좌우로 연결된 2개로 표현된다. 정상적인 곳은 ‘.’으로 표현된다.


## 조건

  - 운동장의 크기 R(1<=R<=100)과 C(1<=C<=100)가 입력된다.
  - R*C의 운동장 정보가 입력된다.

## 입출력 예시

  |R|C|arr|result|
  |:--:|:--:|:--:|:--:|
  |6|7|[.,#,.,.,.,.,#],[.,.,#,.,.,.,#],[.,.,#,.,.,#,.],[.,.,.,#,#,.,.],[.,#,.,.,.,.,#],[.,#,.,.,#,.,#]|8|
  
## 풀이방식

### 나의 풀이방식

  > BFS를 활용한 문제 풀이

  너무 뻔한 문제 풀이 방식으로 진행. 하지만 문제를 제대로 보지 않아서 조건을 제대로 파악하지 못하고 코드가 길어졌다. 왜냐하면 물 웅덩이는 크기가 "1" 아니면 상하좌우로 연결된 "2"개로 표현되기 때문이다.

  ```cpp
  int x[4] = {0,1,0,-1};
  int y[4] = {1,0,-1,0};

  int solution(int n,int m, vector<vector<char>> arr){	
    vector<pair<int,int>> info;
    vector<vector<int>> ch(n,vector<int>(m,0));
    queue<pair<int,int>> Q;
    
    for(int i=0;i<n;i++){
      for(int j=0;j<m;j++)if(arr[i][j] == '#') info.push_back(make_pair(i,j));
    }
    
    int cnt = 1;
    for(int i=0;i<info.size();i++){
      if(ch[info[i].first][info[i].second] > 0) continue;
      Q.push(make_pair(info[i].first, info[i].second));
      
      while(!Q.empty()){
        int tmpX = Q.front().first;
        int tmpY = Q.front().second;
        Q.pop();
        ch[tmpX][tmpY] = cnt;
          
        for(int j=0;j<4;j++){
          int tX = tmpX + x[j];
          int tY = tmpY + y[j];
        
          if(tX >= 0 && tX < n && tY >= 0 && tY < m){
            if(arr[tX][tY] == '.' || ch[tX][tY] > 0) continue;
            else {
              Q.push(make_pair(tX,tY));
              ch[tX][tY] = cnt;
            }
          }
        }
      }
      cnt++;
    }
    return cnt - 1;
  }
  ```

### 다른이의 풀이방식

  > 우측과 아래측만 확인하여 숫자를 증가시키는 방법 

  문제의 리즈를 정확하게 파악한 방법으로 훨씬 간단한 풀이이다.

  ```cpp

  int solution(int n,int m, vector<vector<char>> arr){
    int answer = 0;

    for(int i=0; i<n; i++){
      for(int j=0; j<m; j++){
        if(arr[i][j]=='#'){
          answer++;
          arr[i][j+1]='.';
          arr[i+1][j]='.';
        }
      }
    }

    return answer;
  }
  ```

## 개선점
  - 문제를 제대로 보자
  

## 출처
  ※ 인프런에서 __김태원님의 강의인 "코딩테스트 실전 모의고사(with C++):대기업 대비"를 수강하고,__ 문제를 해결하여 개인적으로 정리한 것입니다. 저작권 및 각종 권한은 지은이에게 있음을 알립니다.