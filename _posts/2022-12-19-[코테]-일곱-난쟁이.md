---
layout: post
title: 코딩테스트 | 일곱 난쟁이
subtitle: DFS
categories: CodingTest
tags: [CodingTest, C++]
---

## 문제

  왕비를 피해 일곱 난쟁이들과 함께 평화롭게 생활하고 있던 백설공주에게 위기가 찾아왔다. 일과를 마치고 돌아온 난쟁이가 일곱 명이 아닌 아홉 명이었던 것이다.

  아홉 명의 난쟁이는 모두 자신이 "백설 공주와 일곱 난쟁이"의 주인공이라고 주장했다. 뛰어난 수학적 직관력을 가지고 있던 백설공주는, 다행스럽게도 일곱 난쟁이의 키의 합이 100이 됨을 기억해 냈다.

  아홉 난쟁이의 키가 주어졌을 때, 백설공주를 도와 일곱 난쟁이를 찾는 프로그램을 작성하시오.

  [링크](https://www.acmicpc.net/problem/2309)
  
## 조건

  - 아홉 개의 줄에 걸쳐 난쟁이들의 키가 주어진다. 주어지는 키는 100을 넘지 않는 자연수이며, 아홉 난쟁이의 키는 모두 다르며, 가능한 정답이 여러 가지인 경우에는 아무거나 출력한다.

## 입출력 예시
  
  |arr|result|
  |:--:|:--:|
  |[20,7,23,19,10,15,25,8,13]|[7,8,10,13,19,20,23]|

## 풀이방식

  > 단순한 DFS 풀이

  종료 조건은 : 합이 100이고, 선택된 난쟁이 수가 7명일때 이다...

  ```cpp
  #include <iostream>
  #include <vector>
  #include <string>
  #include <algorithm>
  using namespace std;

  int ch[9];
  bool isDone = false;

  void DFS(vector<int> &arr, int val, int idx ,int cnt) {
    if (isDone) return;

    if (cnt == 7 && val == 100) {
      for (int i = 0; i < 9; i++) if(ch[i] == 1) cout << arr[i] << "\n";
      isDone = true;
    }
    else if (val > 100 || idx >= 9) return;
    else {
      if (cnt < 7) {
        ch[idx] = 1;
        DFS(arr, val + arr[idx], idx + 1, cnt + 1);
      }

      ch[idx] = 0;
      DFS(arr, val, idx + 1, cnt);
    }
  }

  int main() {
    vector<int> arr(9);

    // INIT
	  for (int i = 0; i < 9; i++) cin >> arr[i];
    sort(arr.begin(), arr.end());

    DFS(arr, 0,0,0);
    return 0;
  }
  ```

## 개선점
  - DFS 굳. 조금 더 깔끔하게 할 수 있을듯