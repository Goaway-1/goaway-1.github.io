---
layout: post
title: 코딩테스트 | [3차] 압축
subtitle: unordered_map, 시뮬레이션
categories: CodingTest
tags: [CodingTest, C++]
---

## 문제

  신입사원 어피치는 카카오톡으로 전송되는 메시지를 압축하여 전송 효율을 높이는 업무를 맡게 되었다. 메시지를 압축하더라도 전달되는 정보가 바뀌어서는 안 되므로, 압축 전의 정보를 완벽하게 복원 가능한 무손실 압축 알고리즘을 구현하기로 했다.

  어피치는 여러 압축 알고리즘 중에서 성능이 좋고 구현이 간단한 LZW(Lempel–Ziv–Welch) 압축을 구현하기로 했다. LZW 압축은 1983년 발표된 알고리즘으로, 이미지 파일 포맷인 GIF 등 다양한 응용에서 사용되었다.

  LZW 압축은 다음 과정을 거친다.

  - 길이가 1인 모든 단어를 포함하도록 사전을 초기화한다.
  - 사전에서 현재 입력과 일치하는 가장 긴 문자열 w를 찾는다.
  - w에 해당하는 사전의 색인 번호를 출력하고, 입력에서 w를 제거한다.
  - 입력에서 처리되지 않은 다음 글자가 남아있다면(c), w+c에 해당하는 단어를 사전에 등록한다.
  - 단계 2로 돌아간다.
  
  압축 알고리즘이 영문 대문자만 처리한다고 할 때, 사전은 다음과 같이 초기화된다. 사전의 색인 번호는 정수값으로 주어지며, 1부터 시작한다고 하자.

  예를 들어 입력으로 KAKAO가 들어온다고 하자.

  1. 현재 사전에는 KAKAO의 첫 글자 K는 등록되어 있으나, 두 번째 글자까지인 KA는 없으므로, 첫 글자 K에 해당하는 색인 번호 11을 출력하고, 다음 글자인 A를 포함한 KA를 사전에 27 번째로 등록한다.
  2. 두 번째 글자 A는 사전에 있으나, 세 번째 글자까지인 AK는 사전에 없으므로, A의 색인 번호 1을 출력하고, AK를 사전에 28 번째로 등록한다.
  3. 세 번째 글자에서 시작하는 KA가 사전에 있으므로, KA에 해당하는 색인 번호 27을 출력하고, 다음 글자 O를 포함한 KAO를 29 번째로 등록한다.
  4. 마지막으로 처리되지 않은 글자 O에 해당하는 색인 번호 15를 출력한다.

  이 과정을 거쳐 다섯 글자의 문장 KAKAO가 4개의 색인 번호 [11, 1, 27, 15]로 압축된다.

## 조건

  - 입력으로 영문 대문자로만 이뤄진 문자열 msg가 주어진다. msg의 길이는 1 글자 이상, 1000 글자 이하이다.
  
## 입출력 예시
  
  |msg|result|
  |:--:|:--:|
  |KAKAO|[11, 1, 27, 15]|
  |TOBEORNOTTOBEORTOBEORNOT|[20, 15, 2, 5, 15, 18, 14, 15, 20, 27, 29, 31, 36, 30, 32, 34]|
  |ABABABABABABABAB|[1, 2, 27, 29, 28, 31, 30]|

## 풀이방식

  > __"unordered_map"을__ 사용하여 새로운 문자열에 번호를 부여

  LZW의 압축과정을 코드로 구현한 것인데, LZM 압축과정의 1번은 무시하고 각 문자가 나타나면 64를 빼주었다.

  2. 현재 사전에서 문자의 최대 길이를 통해 W와 C를 구성
  3. W를 사전에서 탐색하여 존재여부를 확인하는 과정
      - W의 길이가 1이라면 인데스 번호(w[0] - 64)를 answer에 추가하고, 문자열의 최대 길이 갱신 & 사전 추가(w + c)를 진행
      - 길이가 1 이상이라면 Unordered_map에서 탐색하고, 존재한다면 위와 같은 과정을 반복
          - map을 탐색했는데 존재하지 않는다면, w의 길이를 한칸 줄이고, c의 값을 변경하여 다시 탐색

  ```cpp
  #include <string>
  #include <vector>
  #include <unordered_map>

  using namespace std;

  vector<int> solution(string msg) {
    vector<int> answer;
    unordered_map<string, int> M;

    int idxSize = 1, idx = 27;
    for (int i = 0; i < msg.size();) {
      string w = "", c = "";

      // 2번 : 현재 입력가능한 최대 길이의 W와 C
      int j = 0;
      for (; (i + j) < msg.size() && j < idxSize; j++) w += msg[i + j];
      c = msg[i + j];

      // 3번 : w를 map에서 탐색
      while (true) {
        string tmpStr = w + c;
        if (w.size() == 1) {
          answer.push_back((w[0] - 64));
          if (idxSize < tmpStr.size()) idxSize = tmpStr.size();       // 사전내의 문자열 최대 길이
          M[tmpStr] = idx++;                                          // 사전 추가
          i++;
          break;
        }
        else {
          auto it = M.find(w);
          if (it != M.end()) {
            answer.push_back(it->second);
            if (idxSize < tmpStr.size()) idxSize = tmpStr.size();
            M[tmpStr] = idx++;
            i += w.size();
            break;
          }
          // 4번 : map에 없다면 w의 길이를 1칸씩 줄이고, c의 값을 변경
          c = w[(w.size() - 1)];
          w = w.substr(0, w.size() - 1);
          }
      }
    }
    return answer;
  }
  ```

## 개선점
  - 굳