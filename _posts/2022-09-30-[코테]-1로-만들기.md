---
layout: post
title: 코딩테스트 | 1로 만들기
subtitle: DFS, 동적계획법
categories: CodingTest
tags: [CodingTest, C++]
---

## 문제

  정수 X에 사용할 수 있는 연산은 다음과 같이 세 가지 이다.

  1. X가 3으로 나누어 떨어지면, 3으로 나눈다.
  2. X가 2로 나누어 떨어지면, 2로 나눈다.
  3. 1을 뺀다.
  
  정수 N이 주어졌을 때, 위와 같은 연산 세 개를 적절히 사용해서 1을 만들려고 한다. 연산을 사용하는 횟수의 최솟값을 출력하시오.

## 조건

  - 첫째 줄에 1보다 크거나 같고, 106보다 작거나 같은 정수 N이 주어진다.

## 입출력 예시

  |N|result|
  |:--:|:--:|
  |2|1|
  |10|3|

## 풀이방식

### 나의 풀이방식

  > DFS를 활용한 문제 풀이

  단순히 조건을 만족하면 DFS를 반복하는 로직이며, cnt가 ans를 넘어가면 해당 DFS를 중지한다.

  ```cpp
  #include <iostream>
  #include <algorithm>
  using namespace std;

  int ans = 2147483647;
  void DFS(int n, int cnt) {
    if(ans < cnt) return;

    if (n == 1) ans = min(ans, cnt);
    else {
      if (n % 3 == 0) DFS(n / 3, cnt + 1);
      if (n % 2 == 0) DFS(n / 2, cnt + 1);
      DFS(n - 1, cnt + 1);
    }
  }
  int main() {
    int n;
    cin >> n;

    DFS(n,0);
    cout << ans;
    return 0;
  }
  ```

### 다른이의 풀이방식

  > 동적계획법을 사용한 풀이로 이전 값을 활용

  1,2,3번 모두 조건을 만족한다면 진행하고, 가장 작은 값을 적용

  ```cpp
  #include <iostream>
  #include<algorithm>
  using namespace std;
  int DP[1000000];
  int main() {
    int N;
    cin >> N;

    DP[1] = 0;
    for (int i = 2; i <= N; i++) {
      DP[i] = DP[i - 1] + 1;
      if (i % 2 == 0) DP[i] = min(DP[i], DP[i / 2] + 1);
      if (i % 3 == 0) DP[i] = min(DP[i], DP[i / 3] + 1);
    }
    cout << DP[N];
    return 0;
  }
  ```

## 개선점
  - DFS보다는 동적계획법의 풀이가 더 효과적임