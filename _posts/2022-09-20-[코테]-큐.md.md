---
layout: post
title: 코딩테스트 | 큐
subtitle: STL 구현
categories: CodingTest
tags: [CodingTest, C++]
---

## 문제
  
  정수를 저장하는 스택을 구현한 다음, 입력으로 주어지는 명령을 처리하는 프로그램을 작성하시오.

  명령은 총 다섯 가지이다.

  - push X: 정수 X를 스택에 넣는 연산이다.
  - pop: 스택에서 가장 위에 있는 정수를 빼고, 그 수를 출력한다. 만약 스택에 들어있는 정수가 없는 경우에는 -1을 출력한다.
  - size: 스택에 들어있는 정수의 개수를 출력한다.
  - empty: 스택이 비어있으면 1, 아니면 0을 출력한다.
  - top: 스택의 가장 위에 있는 정수를 출력한다. 만약 스택에 들어있는 정수가 없는 경우에는 -1을 출력한다.

## 조건

  - 첫째 줄에 주어지는 명령의 수 N (1 ≤ N ≤ 10,000)이 주어진다. 둘째 줄부터 N개의 줄에는 명령이 하나씩 주어진다. 주어지는 정수는 1보다 크거나 같고, 100,000보다 작거나 같다. 문제에 나와있지 않은 명령이 주어지는 경우는 없다.

## 입출력 예시

  - 입력될때마다 출력하는 방식으로 진행.
 
## 풀이방식

### 나의 풀이방식

  > 단순히 있는 STL(Stack)을 사용하고 싶지 않아서, 배열을 사용하여 스택 클래스 vcc를 선언 

  동작 방식은 기존 STL 사용과 비슷하다고 생각하여, 설명은 생략한다.

  ※ 컴파일러에서는 이상 없이 동작하나, 백준에서는 컴파일에서 발생..!

  ```cpp
  #include <iostream>
  #include <string>
  using namespace std;

  class vcc {
  public:
    int lng = 0, idx = -1;
    int arr[100001];

    void push(int val) {
      arr[++idx] = val;
      lng++;
    }
    int pop() {
      int tmp = arr[idx];
      arr[idx--] = 0;
      if(lng > 0) lng--;
      return (lng < 0) ? -1 : tmp;
    }
    int size() { return lng; }
    int empty() { return (lng <= 0) ? 1 : 0; }
    int top() { return arr[idx]; }
  };

  int main(){
    int n, index = 0;
    vcc arrVC;
    string tmp;
    int tmp2;
    cin >> n;

    while (n--) {
      cin >> tmp;

      if (tmp == "push") {
        cin >> tmp2;
        arrVC.push(tmp2);
      }
      else if (tmp == "pop") cout << arrVC.pop();
      else if (tmp == "size") cout << arrVC.size();
      else if (tmp == "empty") cout << arrVC.empty();
      else if (tmp == "top") cout << arrVC.top();
    }
    return 0;
  }
  ```

### 다른이의 풀이방식

  ```cpp
  #include <iostream>
  #include <string>

  using namespace std;

  int my_stack[10010], top = 0;

  void stack_func(string cmd, int input){
    if(cmd == "push") my_stack[top++] = input;
    else if(cmd == "top"){
      if(top != 0) cout << my_stack[top - 1] << "\n";
      else cout << "-1\n";
    }
    else if(cmd == "empty"){
      if(top == 0) cout << "1\n";
      else cout << "0\n";
    }
    else if(cmd == "size") cout << top << "\n";
    else if(cmd == "pop"){
        if(top == 0) cout << "-1\n";
        else cout << my_stack[--top] << "\n";
    }
  }

  int main(void){
    int N, input;
    cin >> N;
    string cmd;

    for(int i = 0; i < N; i++){
      cin >> cmd;
      if(cmd == "push") cin >> input;
      stack_func(cmd, input);
    }
    return 0;
  }
  ```

## 개선점
  - 생각해보니 난 CPP의 모든 STL을 구현할 줄 아는가...?