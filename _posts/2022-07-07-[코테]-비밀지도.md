---
layout: post
title: 코딩테스트 | 비밀지도
subtitle: 시뮬레이션
categories: CodingTest
tags: [CodingTest, C++]
---

## 문제

  네오는 평소 프로도가 비상금을 숨겨놓는 장소를 알려줄 비밀지도를 손에 넣었다. 그런데 이 비밀지도는 숫자로 암호화되어 있어 위치를 확인하기 위해서는 암호를 해독해야 한다. 다행히 지도 암호를 해독할 방법을 적어놓은 메모도 함께 발견했다.

  1. 지도는 한 변의 길이가 n인 정사각형 배열 형태로, 각 칸은 "공백"(" ") 또는 "벽"("#") 두 종류로 이루어져 있다.
  2. 전체 지도는 두 장의 지도를 겹쳐서 얻을 수 있다. 각각 "지도 1"과 "지도 2"라고 하자. 지도 1 또는 지도 2 중 어느 하나라도 벽인 부분은 전체 지도에서도 벽이다. 지도 1과 지도 2에서 모두 공백인 부분은 전체 지도에서도 공백이다.
  3. "지도 1"과 "지도 2"는 각각 정수 배열로 암호화되어 있다.
  4. 암호화된 배열은 지도의 각 가로줄에서 벽 부분을 1, 공백 부분을 0으로 부호화했을 때 얻어지는 이진수에 해당하는 값의 배열이다.

  네오가 프로도의 비상금을 손에 넣을 수 있도록, 비밀지도의 암호를 해독하는 작업을 도와줄 프로그램을 작성하라.


## 조건

  입력으로 지도의 한 변 크기 n 과 2개의 정수 배열 arr1, arr2가 들어온다.

  - 1 ≦ n ≦ 16
  - arr1, arr2는 길이 n인 정수 배열로 주어진다.
  - 정수 배열의 각 원소 x를 이진수로 변환했을 때의 길이는 n 이하이다. 즉, 0 ≦ x ≦ 2n - 1을 만족한다.
  - 원래의 비밀지도를 해독하여 '#', 공백으로 구성된 문자열 배열로 출력하라.
 
## 입출력 예시

  |n|arr1|arr2|return|
  |:--:|:--:|:--:|:--:|
  |5|[9, 20, 28, 18, 11]|[30, 1, 21, 17, 28]|["#####","# # #", "### #", "# ##", "#####"]|
  |6|[46, 33, 33 ,22, 31, 50]|[27 ,56, 19, 14, 14, 10]|["######", "### #", "## ##", " #### ", " #####", "### # "]|

## 풀이방식

### 나의 풀이방식

  정말 간단하다.

  toTwo()함수를 통해서 입력받는 숫자에 대해서 2진법을 반환하여 arr배열에 저장하였고, 이를 출력하는 방식이다.

  ```cpp
  #include <stack> 
  
  void toTwo(vector<vector<int>> &arr,int n,const int low, int index) {
    stack<int> St;

    while (index > 0) {
      St.push(index % 2);
      index /= 2;
    }

    while (St.size() < n) St.push(0);

    int i = 0;
    while (!St.empty()) {
      if(arr[low][i] == 0) arr[low][i] = St.top();
      i++;
      St.pop();
    }
  }
  vector<string> solution(int n, vector<int> arr1, vector<int> arr2) {
    vector<string> answer;
    vector<vector<int>> arr(n,vector<int>(n,0));

    for (int i = 0; i < n; i++){
      toTwo(arr, n, i, arr1[i]);
      toTwo(arr, n, i, arr2[i]);
    }
    for (int i = 0; i < n; i++) {
      string tmp = "";
      for (int j = 0; j < n; j++) {
        tmp += (arr[i][j] == 1) ? "#" : " ";
      }
      answer.push_back(tmp);
    }
    return answer;
  }
  ```

### 다른 이의 풀이방식

  <span style = "color:orange;">비트연산을</span> 활용한 풀이 방식이다.
  
  먼저 arr1과 arr2를 "|"으로 비트연산을 한다. 그렇게 되면 2번 처리하지 않아도 된다. 그리고 ">>" 시프르연산도 활용하여 비트를 이동시킨다. 이는 나누기 2를 뜻한다. 

  ```c++
  vector<string> solution(int n, vector<int> arr1, vector<int> arr2) {
    vector<string> answer;
    for(int i=0; i <n; i++){
      // 일괄 처리
      arr1[i] = arr1[i]|arr2[i];
      string ans = "";
      for(int j = 0; j<n; j++){
        if(arr1[i] % 2 == 0) ans = " " + ans;
        else ans = "#" + ans;

        // 나누기 2
        arr1[i] = arr1[i] >> 1;
      }
      answer.push_back(ans);
    }
    return answer;
  }
  ```

## 개선점
  - 비트 연산을 활용하자.
  - 기존 제공하는 STL도 좋지만 더욱 효과적인 방법이 있을 수 있다.